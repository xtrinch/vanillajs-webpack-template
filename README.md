# VanillaJS webpack template

Prerequisites:

- `npm install`

To run:

- `npm run serve`

To build:

- `npm run build-dev` or `npm run build-prod`
- Copy bundled `index.html` from `dist/index.html` to wherever it is you want this webpage to live.
